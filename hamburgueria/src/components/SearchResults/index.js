const SearchResults = ({ data, listProduct }) => {
  /*
  if (!data || !data.length) return null;

  const resultList = listProduct.map((item, index) => (
      data
    <li key={index}>{item.name}</li>
  ));

  return (
    <div>
      <ul>{resultList}</ul>
    </div>
  );*/
};

export default SearchResults;
